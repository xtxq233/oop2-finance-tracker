# 工作日志 — oop2-finance-tracker

---

## 2026-06-02（第4次）：实现任务 E — 预算储蓄模块

### 新增文件

| 文件 | 说明 |
|------|------|
| `models/monthly_plan.py` | MonthlyPlan 数据类（储蓄倒推结果） |
| `models/savings_plan.py` | SavingsPlan 数据类（储蓄方法结果） |
| `services/saving_calculator.py` | SavingGoalCalculator（储蓄目标倒推计算器） |
| `rules/__init__.py` | 预算规则族导出 |
| `rules/budget_rule.py` | BudgetRule 抽象接口 |
| `rules/fixed_budget.py` | FixedBudgetRule（固定预算） |
| `rules/rule_503020.py` | Rule503020（50/30/20 规则） |
| `methods/__init__.py` | 储蓄方法族导出 |
| `methods/savings_method.py` | SavingsMethod 抽象接口 |
| `methods/fixed_amount.py` | FixedAmountMethod（固定金额储蓄） |
| `methods/week52.py` | Week52Method（52周存款法） |
| `methods/percentage.py` | PercentageMethod（收入百分比储蓄） |

### 修改文件

| 文件 | 变更 |
|------|------|
| `models/__init__.py` | 新增 MonthlyPlan、SavingsPlan 导出 |

### 验证结果
- SavingGoalCalculator：轻松目标 extra=0，激进目标正确分配削减，空 history 抛 ValueError
- FixedBudgetRule：正确返回预设预算
- Rule503020：50/30/20 分配正确，总和=收入
- FixedAmountMethod(500)：年6000，52周均分
- Week52Method(10)：标准版 10→520，反向版 520→10，年总额13780
- PercentageMethod(0.2)：年4800，52周均分
- models/__init__.py 导出正确

---

## 2026-05-31（第3次）：代码审查修复

### 修复清单

| 优先级 | 问题 | 修改文件 | 状态 |
|--------|------|----------|------|
| P0 | 创建 main_cli.py | 新文件 | ✅ 完成 |
| P0 | "浙江工商大学电费"关键词遮挡→误判为"学习" | category_normalizer.py:113-117 | ✅ 修复 |
| P1 | _import_alipay 编码回退(GBK→UTF-8-sig) | csv_importer.py:204-210 | ✅ 修复 |
| P1 | Alipay统一使用 _parse_amount() | csv_importer.py:257 | ✅ 修复 |
| P2 | 导入后打印"其他"占比 | csv_importer.py:196-198, 279-281 | ✅ 完成 |
| P2 | str \| None → Optional[str] | category_normalizer.py | ✅ 完成 |
| P2 | 行号注释修正 | csv_importer.py:143 | ✅ 完成 |

### 修复后验证
- 浙江工商大学电费 → 日用 ✓（之前误判为学习）
- 微信 520 条 / 支付宝 448 条 数据量不变
- 其他占比：微信 29%，支付宝 16%

---

## 2026-05-31（第2次）：按架构文档重写 CSV 导入模块

### 决策梳理
通读全部内部文档后确认决策链：
- `设计文档.md` + `接口文档.md` = 总体设计契约
- **`架构构思-CSV导入模块.md`** = CSV导入的最终设计（见到真实数据后写的）
- `任务文档-B-集成CSV.md` = 早期简化版，**已被架构文档取代**

### 重写内容

1. **`services/category_normalizer.py`** — 三层映射体系：
   - `normalize_simple()` — 通用字符串映射（50+条规则）
   - `normalize_alipay_category()` — 支付宝交易分类映射（20+条，含跳过列表）
   - `infer_category_wechat()` — 微信关键词推断（5组关键词规则：餐饮/交通/学习/日用/娱乐）

2. **`services/csv_importer.py`** — 完整重写：
   - `_detect_format()` — 自动检测微信/支付宝（先UTF-8-BOM，再GBK，再UTF-8）
   - `_find_header_row()` — 按"交易时间"定位表头（不硬编码行号）
   - `_parse_amount()` — 处理 ¥前缀 + 千位逗号
   - `_parse_date()` — 支持 datetime 格式
   - `_import_wechat()` — 微信11列映射（商户名→推断类别，account=WECHAT）
   - `_import_alipay()` — 支付宝12列映射（交易分类→标准类别，account=ALIPAY）
   - 签名改为 `-> tuple[list[Transaction], int]`

### 真实数据测试

| 指标 | 微信 | 支付宝 |
|------|------|--------|
| 原始行数 | 543 | 876 |
| 导入成功 | 520 | 448 |
| 跳过 | 0(格式错误) + 23(中性交易静默过滤) | 0(格式错误) + 428(不计收支/投资理财过滤) |
| 类别分布 | 餐饮174/日用168/学习31/娱乐7/其他140 | 餐饮70/日用124/交通113/其他93/学习32/娱乐16 |

### 已知问题
- 微信"其他"占27%，主要是转账/红包/群收款，属正常；少量可改进（如医院、门票等需补充关键词）
- C 的 FinanceController.import_csv() 调用接口为 `import_csv(path)` 返回列表，需协调为解包 `transactions, skipped = import_csv(path)`

---

## 2026-05-31（第1次）：初始搭建 — 知识点文档 + 简化CSV导入

### 完成内容

1. **知识点文档** (`内部执行文档/知识点梳理.md`)
2. **models/** — Transaction、AccountType、Budget 临时实现
3. **services/** — 简化版 CSV 导入（已被第2次重写取代）
